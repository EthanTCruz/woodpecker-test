# .woodpecker.yml
when:
  event: [push, pull_request]   # ← add this

steps:
  build:
    image: alpine:3.20
    commands:
      - echo "Hello from Woodpecker 👋"
      - uname -a
      - cat /etc/os-release

